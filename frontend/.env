REACT_APP_API_URL=http://localhost:8000/api
REACT_APP_API_BASE_URL=http://localhost:8000

# Google Analytics 4 Configuration
# CRITICAL: Replace G-XXXXXXXXXX with your real Measurement ID from https://analytics.google.com/
# Without this, all tracking events (likes, shares, searches, UTM tracking) send data to nowhere
# After updating this value, run: make down && make build && make up
REACT_APP_GA4_MEASUREMENT_ID=G-ZWCLBSNDWE